import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  Container: {
    display: "flex",
    // background: "#F5F5F5",
    width:'206vh',
    height:'100vh',
    display:"flex",
    alignItems:'center',
  },
}));
export default useStyles;
